
const SectionTitle = ({title}) => {
    return (
        <h1 className="text-5xl uppercase text-[#00091A] font-bold py-5 text-center">
            {title}
        </h1>
    );
};

export default SectionTitle;